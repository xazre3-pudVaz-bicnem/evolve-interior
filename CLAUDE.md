# 株式会社EVOLVE サイト — プロジェクト固有ルール

ルートの `c:\projects\CLAUDE.md`（Elite Web Agency Master System）に従いつつ、
本プロジェクトでは以下を**上書きルール**として最優先する。

## 事業と目的

兵庫県尼崎市の内装工事会社。軽鉄（軽量鉄骨下地）・ボード工事が主軸。
サイトの目的は2つ。**コーポレート（工事の問い合わせ獲得）と求人（採用応募獲得）を兼ねる。**

1. 一般のお客様・建設会社・工務店から内装工事の問い合わせを獲得する
2. 未経験者と経験者の両方から採用応募を獲得する

軽鉄・ボードを知らない一般の方にも、写真と文章で直感的に伝わることを最優先する。

## 絶対に守ること（捏造禁止）

支給されていない情報は**絶対に作らない**。以下はすべて未提供。

- 会社: 資本金 / 設立年月日 / 従業員数 / 建設業許可番号
- 実績: 実績年数 / 施工件数 / 施工場所 / 施工時期 / 施主
- 保証・対応: 資格 / 保証 / 「最短」「即日」「無料」

**採用条件は 2026-07-15 にEVOLVE社から提供された**（下表）。値は `lib/recruit.ts` に集約。
ただし**提供された粒度を超えて具体化しない**。次は未提供なので勝手に足さない:
年間休日数 / 賞与の額・回数 / 昇給の額・時期 / 試用期間の有無・期間 /
交通費の上限額 / 残業時間の実績 / 資格手当の額 / 寮・社宅。

過去に「業界の平均に合わせて」と指示されたが、平均値での代筆は**しなかった**。
実際の労働条件は平均値で代用できず、虚偽の求人条件は職業安定法第65条違反
（6ヶ月以下の懲役または30万円以下の罰金）で、罰を受けるのはEVOLVE社、騙されるのは応募者。
その後EVOLVE社が実値を提供したので、**その実値のみ**を掲載している。

禁止表現: 「業界最安」「地域No.1」「必ず高品質」「アットホーム」「稼げる」「誰でも簡単」
**「すべて自社施工」とも書かない**（協力会社と連携する場合があるため）。

年齢について、応募制限と誤認される断定表現は避け、次の言い方に統一する:
「10代後半から20代の若手スタッフを中心に、幅広い年代の方からの応募を歓迎しています。」

対応可否が現場条件に依存する事項は断定せず「まずはご相談ください」に留める。

## 確定情報（唯一の情報源は `lib/constants.ts`）

| 項目 | 値 |
| --- | --- |
| 会社名 | 株式会社EVOLVE |
| 代表者 | 元田 健司郎 |
| 所在地 | 兵庫県尼崎市大庄中通5-27-6 |
| 電話 | 080-2523-6928 |
| メール | evolve.see0906@ymail.ne.jp |
| 問い合わせ受付 | 08:30〜18:30 |
| 雇用形態（採用） | 正社員 |
| 勤務時間の目安 | 08:30〜17:30（採用） |
| Instagram | `https://www.instagram.com/evolv_e96/?hl=ja` |
| LINE | `https://line.me/ti/p/cd7hEovSUJ#~` |
| 対応エリア | 兵庫・大阪・京都・滋賀を中心とした関西エリア（姫路方面〜滋賀方面） |

### 採用条件（2026-07-15 EVOLVE社提供。情報源は `lib/recruit.ts`）

| 項目 | 値 |
| --- | --- |
| 給与 | 月給 26万円〜30万円（経験・技術を考慮のうえ決定） |
| 休日・休暇 | 週休2日制 |
| 待遇・福利厚生 | 社会保険完備 / 昇給 / 賞与 / 交通費支給 / 作業服・道具支給 / 資格取得支援 / 残業手当 |
| 選考の流れ | 応募 → 電話またはメール連絡 → 面接1回 → 採否連絡 → 入社 |

上記が揃ったため **JobPosting 構造化データを `/recruit/requirements` に出力**している
（`buildJobPostingSchema()`。値のどれかを `null` に戻せば自動で出力停止）。

## LINE 導線

内装工事の相談では「現場の写真を送りたい」というニーズが強く、メールより LINE が使われる。
若い求職者にとってもメール・電話よりハードルが低い。そのため**問い合わせと採用の両方**に置いている。

設置場所：ヘッダー / モバイル固定バー / モバイルメニュー / フッター /
共通CTA（ContactCta）/ お問い合わせページ / 応募ページ / 採用ページ /
**フォーム送信後のパネル（メールソフトが開かない環境の受け皿）** / フォームのエラーパネル

- **LINE緑 `#06C755` は白文字だと 2.26:1 で読めない。**
  濃色文字（`ink-900`）を乗せて 8.4:1 にすること（`components/ui/LineButton.tsx`）。
  白文字のLINEボタンを作らないこと。

**NAPはページに直書きしない。必ず `COMPANY` を参照する。**
構造化データとサイト表記は完全一致させること。

## データの置き場所

| ファイル | 内容 |
| --- | --- |
| `lib/constants.ts` | 会社情報・ナビ・エリア（**単一情報源**） |
| `lib/services.ts` | 工事9種の全コンテンツ + 見積もりの流れ |
| `lib/works.ts` | 施工実績7件（支給写真から確認できる内容のみ。空にすると /works は自動で「準備中」表示に戻る） |
| `lib/recruit.ts` | 募集要項・一日の流れ・JobPosting準備 |
| `lib/faqs.ts` | 集客FAQ / 採用FAQ |
| `lib/seo.ts` | metadata生成 + 構造化データ |
| `lib/media.ts` | 画像のビルド時解決（置くだけ差し替え） |

## 実装上の要点

- **写真は「置くだけ差し替え」**。`public/images/` にファイルを置いて再ビルドすると、
  仮枠（`PhotoFrame`）が自動で実写に切り替わる。コード変更不要。ファイル名は README 参照。
- **フォームは `mailto` 方式が既定**（`lib/constants.ts` の `FORM_MODE`）。
  既存プロジェクト（染 YUI COLORS）と同じく、サーバー設定も外部サービスも不要。
  訪問者のメールソフトが開くだけなので、**「送信しました」とは書かない**
  （「メールソフトを開きました。内容を確認して送信してください」と表示する）。
  - **`lib/formMail.ts` の本文に全角スペースや区切り線などの装飾を入れないこと。**
    日本語1文字は `%XX%XX%XX`（9文字）に展開されるため、全角スペース1つで9文字ぶん膨らむ。
    Outlook等は約2000文字で本文を切り落とすので、装飾だけで本文が消える。
  - メールソフトが開かない環境に備え、完了画面には必ず
    「入力内容をコピーするボタン」と電話番号を併記する。
- **`FORM_MODE` を `'api'` にする場合**（Resend設定時）も、
  **送れていないのに成功表示しない**。メール未設定なら 503、送信失敗なら 502 を返し、
  画面には電話・メールの代替導線を出す。
- **JobPosting は `/recruit/requirements` に出力済み**（2026-07-15に給与が確定したため）。
  `lib/recruit.ts` の `JOB_POSTING_DATA` を参照。値のどれかを `null` に戻すと自動で出力が止まる。
  給与・雇用形態・待遇は画面表示と構造化データを一致させること（Googleしごと検索の要件）。
- **Googleマップは `/company` に埋め込み済み**。EVOLVE社のビジネスプロフィールと
  住所の一致を確認したうえで設置し、`lib/constants.ts` の `GEO` に実測の緯度経度を、
  構造化データに `geo` / `hasMap` を入れている。
  - **`share.google/...` の共有リンクをそのままリンク先に使わないこと。**
    開くとマップではなく**検索結果ページ**に飛ぶ。
    `https://www.google.com/maps/search/?api=1&query=...`（公式のURLスキーム）を使う。
- **営業曜日が不明**なため、構造化データに `openingHoursSpecification` を含めない。
  「問い合わせ受付時間 08:30〜18:30」は本文にのみ記載する。
- **施工実績の施工場所・施工時期・対象区分は空のまま**。写真から判断できないため。
  推測で埋めてはいけない（`lib/works.ts` の任意項目）。
- **AI生成画像は7点**（`customer.jpg` / `corporate.jpg` / `banner-services.jpg` /
  `service-kitchen-panel.jpg` / `service-wallpaper.jpg` / `service-painting.jpg` /
  `day-break.jpg`）。実際の現場・スタッフではないため、**施工実績には絶対に使わない**。
- **代表者の氏名で署名した「代表メッセージ」を書かない。**
  本人が書いていない文章を、実在する個人の名前で公開することになる。
  会社としての考え方（`/company` の OUR APPROACH）として書き、署名は入れていない。
- **ヒーローにCTAボタンを置かない**。施工写真とキャッチコピーが主役。

## デザイン規約

- ベース白。緑は**要所のみ**。過剰なグラデーション禁止。
- **角丸ボックスで全セクションを囲まない**。ロゴ由来の「斜め」を共通モチーフにし、
  直線とヘアライン（`gap-px` + 背景色）で区切る。カードを並べるだけにしない。

### コントラスト規約（すべて実測して決めた値。勝手に薄くしない）

| 色 | 白背景での比 | 用途 |
| --- | --- | --- |
| `brand-500` #00A651 | 3.2:1 | **図形・罫線・アイコンのみ。文字に使わない** |
| `brand-700` #0B6E3A | 6.4:1 | 緑の文字・緑ボタン（白抜き文字） |
| `ink-400` #8B9298 | 3.2:1 | **大きい文字（24px以上）と枠線のみ。小さい文字に使わない** |
| `ink-500` #6B7278 | 4.9:1 | 小さい文字（本文・注記・キャプション・プレースホルダ） |
| `mist-300` #D9DEDB | 1.4:1 | **文字にも枠線にも使わない**（区切り線・面のみ） |
| `mist-400` #C3CAC6 | 濃色背景で11:1 | 濃色背景の上の文字のみ |

- **フォームの入力欄・ボタンの枠線は `ink-400` 以上**（枠線が唯一の識別手段なので3:1必要／WCAG 1.4.11）。
  `mist-300` は 1.4:1 しかなく不可。
- **ヒーロー上の透過ヘッダー**は、ヘッダー自身のグラデーションだけでは白文字が読めない
  （hero.jpg は窓が白飛びしていて 2〜3:1 しか出なかった）。
  `components/home/Hero.tsx` 側にもヘッダー用スクリムを敷き、2枚重ねで 15:1 を確保している。
  **どちらか一方でも外すと文字が写真に埋もれる。**
- スマホ最優先。画面下部に固定導線（電話／工事の相談／採用応募）。
  `body` に `pb-16` を入れて本文が隠れないようにしている。

## アニメーションの落とし穴（すべて実際に踏んだ不具合）

演出は `prefers-reduced-motion` を尊重し、**JS無効でも内容が必ず表示される**こと。

1. **`.reveal` の初期 `opacity:0` は `.js` が付いているときだけ適用する。**
   無条件に効かせると、JS無効時に本文が永久に見えなくなる。
   `.js` は `layout.tsx` のインラインスクリプトが描画前に付与する。

2. **監視対象の要素を `clip-path` で隠してはいけない。**
   完全にクリップされた要素は IntersectionObserver が「見えていない」と判断するため、
   `.is-visible` が永久に付かず写真が出てこない（中の画像の遅延読み込みも走らない）。
   `clip` 演出は、要素の上に乗せた「幕」（`::after`）だけを動かして実現している。

3. **`main` の `overflow-x` は `clip`。`hidden` にしてはいけない。**
   左右にずらす演出（`data-anim="left" / "right"`）が画面幅をわずかに超えるため
   封じ込めが要るが、`hidden` はスクロールコンテナを作ってしまい
   中の `position: sticky` が効かなくなる。

4. **ヘッダー内に `position: fixed` の要素を置かない。**
   ヘッダーには `backdrop-blur` が掛かっており、`backdrop-filter` は
   `fixed` の包含ブロックを作るため、パネルの高さが潰れて表示されなくなる。
   モバイルメニューは `createPortal` で `<body>` 直下に出している。

5. **ローディング画面は初回訪問のみ**（sessionStorage）。
   `<html>` のクラス（`.loading` / `.loaded`）で制御し、
   2回目以降は描画前の時点で非表示にする（ちらつき防止）。

6. **ローディング画面のスクロール固定は、CSSではなくJSで行う。**
   `.loading` を付けるのはインラインスクリプトだが、外すのは Loader の useEffect だけ。
   CSSで `body { overflow: hidden }` を掛けると、JSバンドルの読み込みに失敗したときに
   解除されず、**真っ黒な画面のまま操作不能**になる（本文はSSR済みなのに読めない）。
   さらに CSS 側に退場アニメーション（`loaderFailsafe`）を入れ、
   JSが動かなくても必ず消えるようにしてある。**この保険を外さないこと。**

## アクセシビリティの落とし穴（すべて実際に踏んだ不具合）

1. **`<section aria-labelledby="x">` を使うなら、`SectionHeading` に `id="x"` を必ず渡す。**
   渡し忘れると参照先が存在せず、そのセクションが**ランドマークとして認識されなくなる**
   （axe/Lighthouse でもエラー）。以前17箇所で参照切れになっていた。

2. **`aria-invalid` は `<fieldset>`（role=group）では無視される。**
   ラジオは `role="radiogroup"` にする。チェックボックス群は各 input 側に付ける。

3. **`sr-only` の input はフォーカスリングが見えない。**
   ラジオのチップには `has-[:focus-visible]:ring-2` を付けること（`CHIP_BASE`）。

4. **入力不備のときは、スクロールだけでなくフォーカスも移す**（`focusFirstError()`）。
   スクロールだけだとキーボード利用者のフォーカスは送信ボタンに残る。

5. **モバイルメニューを開いている間は、背面（main / footer / 固定バー / スキップリンク）を
   `inert` にし、フォーカスをトラップする。** やらないと、覆われて見えないリンクに
   Tabで到達できてしまう。

6. **モバイルメニューのリンクには必ず `onClick={close}` を付ける。**
   「いま見ているページ」と同じリンクを押すと `pathname` が変わらず、
   メニューが開いたまま・スクロール固定されたままになる。

7. **ホバーで開くメニューは `onPointerEnter` で `pointerType === 'mouse'` を判定する。**
   `onMouseEnter` はタッチでも発火するため、タップすると
   「ホバーで開く → クリックでトグル → 閉じる」となり、何も起きないように見える。

## 品質チェック

```bash
npm run build   # 型エラー・ビルドエラーがないこと
npx eslint .    # 0 problems であること
```
